<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <record id="ir_cron_sync_miracle_stock" model="ir.cron">
            <field name="name">Miracle: Sync Product Ledger Stock</field>
            <field name="model_id" ref="base.model_res_company"/>
            <field name="state">code</field>
            <field name="code">
for company in model.search([('miracle_clientid', '!=', False)]):
    try:
        company._action_get_miracle_product_ledger()
    except Exception as e:
        log("Failed to sync miracle stock for company %s: %s" % (company.name, e), level='error')
            </field>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">1</field>
            <field name="interval_type">hours</field>
            <field name="numbercall">-1</field>
            <field name="doall" eval="False"/>
            <field name="active" eval="False"/>
        </record>
    </data>
</odoo>
